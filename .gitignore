node_modules/
dist/
release/
test-results/
playwright-report/
.DS_Store
*.log
.env
electron-builder.env
*.omc
eval/
models/
native/speech-helper/speech-helper
# Private licensed Animalese voice clips (CLA-53): bundled into dist/ at build
# time by Vite, but must never enter the public repo. The engine degrades to
# silent when they are absent (public checkout / CI / tests).
src/renderer/assets/voice/*.wav
src/renderer/assets/voice/*.mp3
src/renderer/assets/voice/*.ogg
src/renderer/assets/voice/*.aiff
src/renderer/assets/voice/*.m4a
src/renderer/assets/voice/*.flac
proxy/.wrangler/
.claude/settings.local.json
